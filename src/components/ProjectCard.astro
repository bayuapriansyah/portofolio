---
const { image, title, desc } = Astro.props;
---

<div class="rounded-2xl p-6 border border-dashed border-gray-300 mb-10">
  <img src={image} alt="" class="rounded-md mb-4" />
  <h3 class="font-semibold text-xl text-gray-900">{title}</h3>
  <p class="text-gray-600 mt-1">{desc}</p>
</div>
